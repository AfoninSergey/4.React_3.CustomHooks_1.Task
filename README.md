*Модуль 4.React / 3.Пользовательские хуки
Задание #1
Реализуйте хук useFetch(), который можно будет использовать следующим образом: (дан шаблон)

Решение: (*сразу TypeScript)
1. В созданном файле useFetch.ts создал (экспортировал) функцию useFetch и т.к. принимает она адрес, добавил в аргумент "url: string".
2. Создал при помощи useState() и экспортировал все состояния, которые принимаются (деструктуризируются) в шаблоне задания.
3. В useEffect реализовал fetch запрос, в котором в нужные моменты задаюстя состояния полученных данных, загрузки и ошибки (ошибку отлавливаю как в catch так и в then, если response не ок)
*На этом этапе проверил работоспособность приложения. Список тайтлов, полученный с сервера отображается, кнопка отображается, информация о загрузке и об ошике (если "подправить" url) тоже работают корректно
**Осталось решить вопрос с кнопкой, которая вызывает функцию refetch с переданными параметрами (тут немного "сломал себе голову"). Изначально вывелся весь полученный список постов. В задании по этому поводу ничего не сказано, но т.к. в фунцкию refetch передается параметр _limit: 3, предположил, что выводить на страницу нужно только три тайтла (причем, скорее всего, при нажатии кнопки должны вывестись следующие три и т.д.)
4. Переделал фетч и состояния. Добавил в состояния текущий индекс (массива постов), а также добавил еще один стэйт - все данные, полученные с сервера. А так же в параметры добавли лимит постов (тип number, по умолчанию - 3, на случай, если не передается в хук, как в нашем примере из задания).
Теперь первое срабатывание fetch в хуке useEffect добавляет в состояния полный список постов (чтобы потом с ним работать без запросов на сервер), лимитированный список для вывода на страницу начиная с первого (т.е. с 0 индекса) и сразу меняет начальный индекс на limit (в наше случае на 3)
*Тут немного подумал и добавил условие, менять начальный индекс на limit, если длина массива, полученного с сервера больше limitа, иначе какой смысле, если в ответе будет 3 поста или меньше... 
5. Реализовал функцию refetch, в которой после проверки, что у нас есть массив данных с которым можно работать и что нам пришли нужные параметры (лимит) в список для отображения на странице добавляется следующих три поста из списка, полученного с сервера и меняется начальный индекс, с проверкой, что если список закончился, то список начинает отображаться сначала (т.е. индекс становится 0)
6. Проверил работоспособность приложения:
- пролистывание постов
- количество выводимых постов, в зависимости от переданного параметра в refetch
- отображения списка сначала, если дошел до конца списка
7. Типизировал получаемые данные с сервера, т.к. отображалась ошибка в переборе списка map